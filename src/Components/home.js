import React from "react";

import './CSS/home.css';

const Home = () => {
    return (
        <div id="homePage">
            <h1 id="siteTitle">Flashback to the 90's </h1>
        </div>
    );
}

export default Home;